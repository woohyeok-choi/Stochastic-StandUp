package kaist.iclab.standup.smi.ui.timeline

import android.os.Bundle
import kaist.iclab.standup.smi.base.BaseViewModel

class TimelineViewModel(navigator: TimelineNavigator) : BaseViewModel<TimelineNavigator>(navigator) {
    override suspend fun onLoad(extras: Bundle?) {
        
    }
}