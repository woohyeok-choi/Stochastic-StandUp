<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>
        <variable
            name="name"
            type="String" />

    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <TextView
            android:id="@+id/txt_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingBottom="@dimen/space_vertical_small"
            android:text="@string/timeline_place_rename_title"
            android:textColor="@color/black"
            android:textSize="@dimen/txt_size_medium" />

        <EditText
            android:id="@+id/edt_rename"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@drawable/bg_selectable_text_view"
            android:hint="@string/timeline_place_rename_hint"
            android:text="@{name}"
            android:textColor="@color/grey"
            android:textColorHint="@color/light_grey"
            android:textSize="@dimen/txt_size_default"
            android:inputType="text" />
    </LinearLayout>

</layout>