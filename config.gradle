def config = [:]

config.version_code = 14
config.version_name = "0.1.4"

config.pref_name = "stand-up-pref"
config.remote_pref_name = "stand-up-pref-remote"
config.firebase_project_id = "standup-stochastic-incentive"

config.firebase_test_mode = true

ext.config = config